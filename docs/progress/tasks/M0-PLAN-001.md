# M0-PLAN-001 OralFlow M0 工程骨架实施计划

## 1. 任务状态

- 状态：`completed`
- 当前角色：`acceptor`
- 所属里程碑：M0
- 创建时间：2026-07-31（追溯记录；原始精确时间未落盘）
- 更新时间：2026-07-31 17:44 +08:00
- Git 分支：`main`
- 最终 Commit：`pending`（本任务原本为只读规划；任务卡由 `M0-RECORD-001` 追溯创建）

## 2. 原始需求

以下内容保留原始任务的要求，不将后续实现结果伪装成当时已存在的记录：

> 任务编号：M0-PLAN-001
>
> 任务名称：OralFlow M0 工程骨架实施计划
>
> 当前位于 OralFlow 项目根目录。本轮只进行 M0 的规划与仓库检查，不允许修改任何文件。
>
> 必须先读取 `docs/development-spec.md`、当前仓库的 `AGENTS.md` 和 `README.md`（如果存在），并检查 Git 状态与目录结构；如果开发规范不存在则停止。
>
> 只读检查当前目录、仓库初始化与清洁状态、已有文件与冲突，以及 Node.js、npm、Python、Conda、目标 Conda 环境和包配置文件。
>
> 为 M0“工程 Harness 与契约冻结”输出可逐项执行的最小开发循环；每个循环必须包含任务编号、目标、原因、依赖、必读文件、允许与禁止范围、文件、实现、验证、验收、失败处理和用户确认。
>
> 计划必须说明 Workflow、Node、Role、Run、Event 的引用与版本关系，统一节点契约，边类型，循环与子工作流限制，7+1 角色、Observer/Supervisor 边界，以及 Codex/Mock Adapter 边界。
>
> 不得创建、修改或删除文件，不得提交 Git、安装依赖、初始化框架、实现 GUI、Runtime、英语训练或真实模型集成，也不得扩大到 M1。
>
> 输出仓库与环境检查、问题、推荐目录、最小循环、详细任务卡、对象关系、测试验收、后续命令、待确认决策和首个推荐任务，然后停止等待确认。

## 3. 任务目标

在不修改仓库的前提下，检查 M0 起点并形成连续、范围受限、可以独立验证的实施计划。

## 4. 范围

### 允许修改

- 无。仅允许读取仓库、Git 元数据和本地开发环境状态。

### 禁止修改

- 整个仓库及 Git 历史。
- 系统与项目依赖。
- GUI、Runtime、Agent、英语训练和模型集成。

## 5. 前置依赖

- `docs/development-spec.md` 存在。
- 可以读取仓库目录与 Git 状态。
- 用户提供 M0 技术栈和范围约束。

## 6. 实施计划

原始对话中的详细计划没有在当时落盘。根据原始需求、随后批准的实施顺序和 Git 证据，可追溯的计划主线为：

1. 仓库治理与工程约束。
2. 环境和基础配置。
3. 架构、DSL、节点与角色契约文档。
4. 版本化 JSON Schema。
5. 最小根工作流和子工作流示例。
6. 离线 Schema/图验证器。
7. 正反例自动测试。
8. 前后端空骨架与 Mock Adapter。
9. Windows CI 与质量检查。
10. 本地和远端 M0 总体验收。

## 7. 验收标准

- 完成所有必需的只读检查。
- 给出连续、原子、可单独验收的 M0 任务循环。
- 每个循环声明允许/禁止范围和可复现验证方式。
- 不产生文件、依赖、Git 或外部系统修改。
- 停止并等待用户批准后续实施。

## 8. 实际实施记录

| 时间 | 角色 | 动作 | 结果 |
|---|---|---|---|
| 2026-07-31 | Planner | 读取开发规范并检查仓库、Git 和环境 | 完成 |
| 2026-07-31 | Planner | 输出 M0 最小开发循环和重点契约设计 | 完成 |
| 2026-07-31 | User | 指示继续，并授权后续 Git 作者配置和开发 | 计划获准进入实施 |

## 9. 文件变更

| 文件 | 操作 | 说明 |
|---|---|---|
| 无 | 无 | 原任务明确要求只读 |

本文件是 `M0-RECORD-001` 的追溯性记录，不是 `M0-PLAN-001` 当时产生的文件。

## 10. 测试记录

| 检查 | 结果 | 证据 |
|---|---|---|
| Git 状态和目录检查 | 通过 | 原始对话检查结果 |
| 环境可识别性检查 | 通过并记录差异 | 原始对话检查结果 |
| 无写入约束 | 通过 | 规划轮未创建或修改项目文件 |

## 11. Observer 记录

- 主要偏离：项目在开发台账建立前完成了 M0 实施，因此本任务卡只能追溯创建。
- 原始规划对话仍是补充证据，但不再作为唯一项目依据。
- 不确定项：原始规划回复的逐字文本未落盘；本文件只记录能够由请求、Git 和验收证据支持的事实。

## 12. Reviewer 结论

- 结论：`conditional`
- 审核发现：计划覆盖了 M0 范围、契约、验证和隔离要求；缺陷是没有同期任务卡。
- 处理：由 `M0-RECORD-001` 补建追溯记录，并明确标识其追溯性质。
- 证据：原始需求、M0 有界提交历史、`docs/m0-acceptance.md`。

## 13. Supervisor 结论

- 决策：`ACCEPT`
- 记录完整性：`complete_with_retrospective_limitation`
- 原因：只读计划已被用户批准并产生了可追溯的后续 M0 提交；历史记录缺口已显式披露。
- 人工升级条件：如需逐字恢复原始规划输出，必须由用户提供对话导出，不得推测补写。

## 14. 验收结果

- 结果：`passed`
- 证据：用户指示继续实施；随后 M0 本地检查及 `M0 Quality Gate #1` 通过。
- 遗留问题：任务卡为追溯记录，不具备同期创建时间和原始规划全文。
- 后续任务：`M0-RECORD-001`。

## 15. Git 信息

- 分支：`main`
- 原任务 Commit：不适用，原任务按要求只读。
- 追溯任务卡 Commit：`pending`
- 相关 M0 提交：`6a17c79` 至 `2809b50`
- 远端状态：`2809b50` 尚未推送；其父提交已在 `origin/main` 通过 hosted CI。

## 16. 后续任务

- 完成并验收 `M0-RECORD-001`。
- 网络恢复并获得提交授权后，记录并推送开发台账提交。
- 不在本任务中启动 M1。

## 17. 最终摘要

M0-PLAN-001 完成了只读仓库检查和 M0 实施规划，并得到用户继续实施的授权。本任务没有代码或文件改动；当前任务卡是为消除历史记录缺口而创建的明确追溯记录。
