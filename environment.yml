name: oralflow

channels:
  - conda-forge

dependencies:
  - python=3.12
  - pip
  - pip:
      - "fastapi>=0.115,<1.0"
      - "httpx>=0.27,<1.0"
      - "jsonschema[format]>=4.23,<5.0"
      - "mypy>=1.14,<2.0"
      - "pydantic>=2.10,<3.0"
      - "pytest>=8.3,<10.0"
      - "pytest-cov>=5.0,<8.0"
      - "referencing>=0.36,<1.0"
      - "ruff>=0.9,<1.0"
      - "types-jsonschema>=4.23,<5.0"
      - "uvicorn>=0.34,<1.0"
